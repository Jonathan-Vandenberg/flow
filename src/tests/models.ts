import { v4 as uuidv4 } from 'uuid';
const organisationId = uuidv4()
const directoryId = uuidv4();

const organisation = {
    id: organisationId,
    name: "",
    country: "",
    subStatus: "",
    imageUrl: "",
    createdAt: "",
    updatedAt: "",
}
